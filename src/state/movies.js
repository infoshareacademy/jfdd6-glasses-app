const initialState = {

}

const reducer = (state = initialState, action = {}) => {
  switch(action.type) {
    case 'TODO':
      return {

      }
    default:
      return state
  }
}

export default reducer